#include "Gradebook.h"

Gradebook::Gradebook() {
    this->grades = nullptr;
    this->gradeCount = 0;
}

void Gradebook::addGrade(int studentId, double score) {
    // Implement grade addition logic here
    // You may need to allocate memory for grades, add the grade, etc.
}

// Other methods for Gradebook
